user: test

targets:
  remark42:
    hosts: [{host: "h1.example.com", name: "host1"}, {host: "h2.example.com", name: "host2"}]

tasks:
  - name: simple_task
    commands:
      - name: echo test
        script: echo "test message"
      
      - name: list tmp
        script: ls /tmp | head -5