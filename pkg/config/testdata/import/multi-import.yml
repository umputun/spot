user: testuser
targets:
  prod:
    groups: ["production"]

tasks:
  - import: tasks/prepare.yml
  - import: tasks/check-task.yml
  - name: final-check
    commands:
      - name: done
        script: echo done
